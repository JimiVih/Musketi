using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class crouchScrir : MonoBehaviour
{
    Animator plrAnim;


    // Start is called before the first frame update
    void Start()
    {
        plrAnim = GetComponent<Animator>();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
